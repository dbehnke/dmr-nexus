# Git
# Include .git in build context so build-time git metadata can be computed inside the builder.
# .gitignore remains useful locally but should not be excluded from the build context when you
# need embedded version information.

# Build artifacts
bin/
dist/
*.exe
*.dll
*.so
*.dylib

# Testing
coverage.txt
coverage.html
*.test

# IDE
.idea
.vscode
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Dependencies (will be downloaded in container)
vendor/

# Frontend (except dist which we copy)
frontend/node_modules
frontend/.vite
frontend/.env.local

# Documentation (not needed in container)
*.md
docs/

# CI/CD
.github/
.dagger/

# Docker files (not needed in build context)
docker-compose*.yml
.dockerignore

# Logs
*.log
logs/

# Temporary files
tmp/
temp/
*.tmp

# Config files
config.yaml
*.local.yaml

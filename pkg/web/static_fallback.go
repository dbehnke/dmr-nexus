//go:build ignore
// +build ignore

package web

// This file is preserved for reference but ignored by the build. The real
// fallback implementation lives in static.go and the embed-based
// implementation is in static_embed.go (build tag 'embed').
